# Next Build (single approved item)

| Field | Value |
|---|---|
| Build ID | BUILD-NEXT-hero-position-fix |
| Objective | Once `[diag]` console logs are captured and analyzed, implement the confirmed fix for BUG-001 (hero mispositioning on page 3 of Build AI Draft output) |
| Scope | Whatever the log analysis confirms as root cause — limited to the hero-fit/serialize path in `OCF_START.html` (`buildAIDraft`, `applySlotPhoto`, `loadHero`, `fitHeroToFrame`, `serializeActivePage`) |
| Acceptance criteria | Every page generated by Build AI Draft shows its hero photo in the correct fitted position, on screen and in exported PNG, including page 3 |
| Depends on | User capturing and providing `[diag]` console output; analysis against the four-way split in `06_BUG_TRACKER.md` |

Do not begin implementation until the diagnostic logs have been reviewed and the root cause confirmed.
