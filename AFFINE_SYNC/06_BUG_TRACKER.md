# Bug Tracker

## BUG-001
| Field | Value |
|---|---|
| Module | Auto Layout / Build AI Draft |
| Description | Hero photo renders in an incorrect position on page 3 of a multi-page draft generated by `buildAIDraft()` |
| Severity | High — affects core Auto Layout workflow output |
| Status | OPEN — diagnosis in progress, logs not yet captured |
| Reproduction steps | 1. Upload multiple photos. 2. Let Hero Analysis produce a recommendation. 3. Click "Build AI Draft" to generate a multi-page project. 4. Navigate to page 3. 5. Observe hero photo position does not match its fitted frame. |
| Root cause | Not yet confirmed. Leading hypothesis (unconfirmed, pending log review): `loadHero()` sets `hero.x/y/w/h` asynchronously via `heroImg.onload → fitHeroToFrame()`, while `buildAIDraft()`'s page loop calls `serializeActivePage()` synchronously right after `renderComposition()`, which may capture stale hero geometry before the image-load fit completes. This is hypothesis (b)/(c) in the four-way diagnostic split below — **not confirmed**. |
| Fix commit | — (no fix applied; no code changes authorized until diagnostic logs are reviewed) |
| Retest result | — |

**Diagnostic split being investigated (see `AFFINE_SYNC/01_CURRENT_BUILD.md`):**
1. Does `renderComposition()` receive the correct input for page 3?
2. Is hero geometry updated before `serializeActivePage()` runs?
3. Does `serializeActivePage()` capture stale geometry?
4. Does geometry change after serialization?
