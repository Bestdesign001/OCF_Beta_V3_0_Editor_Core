# Changelog

## 2026-07-30 — 57abfdc
**Build:** BUILD-2026.07.30-automation-pipeline (completed)
**Summary:** Fixed BUG-002 — implemented the locked automation pipeline (automatic text extraction, hero/photo distribution, content-driven multi-page generation, Facebook/TikTok captions, Export All Pages) that a real-user test found largely missing, and fixed two further defects the new functionality exposed.

- **Added:** `extractEventInfo()` (Date/Time/Location/Department/Activity/Result/Public Information/Closing extraction, labelled + unstructured fallback), `generateFacebookCaption()`/`generateTikTokCaption()`, `exportAllPages()`, `getLayerText()`/`setLayerText()`, Event Details + Facebook/TikTok caption UI panel, Photo Queue "Unused: reason" badge, `lastUnassignedPhotos`, `CONTENT_SECTIONS`.
- **Changed:** `planPages()` now generates content-section-driven pages (Cover/Activity/Results/Public Information/Closing) instead of generic photo-count chunking; `serializeActivePage()` now reads title/subtitle from the rendered layer text and carries `type`/`section`; `buildAIDraft()` tags each page with its section; `updateText()` and `exportFilename()`/`exportPNG()` updated to support the above.
- **Fixed:** BUG-002 (missing automation pipeline). Also fixed two pre-existing defects exposed by it: `updateText()`'s `.textContent=` was deleting the title/subtitle layers' resize/rotate handles on every edit; `clearSlot('heroLayer')` never reset `heroIndex`, so hero-less pages inherited the previous page's hero.
- **Removed:** Nothing.
- **Files changed:** `OCF_START.html`
- **Git commit:** `57abfdc93de054cf9ad87690bd3b17c3fb6584c5`
- **Verification:** Live browser test, real file uploads + real event text — full end-to-end acceptance flow passes. See `05_TEST_RESULTS.md`.

## 2026-07-30 — 3decadb
**Build:** BUILD-2026.07.30-hero-position-diag (completed)
**Summary:** Fixed BUG-001 — hero photo mispositioned and, in some cases, misidentified across pages generated by `buildAIDraft()`. Removed the temporary `[diag]` logging now that the fix is verified.

- **Added:** Nothing.
- **Changed:** `loadHero()`/`fitHeroToFrame()` now accept known photo width/height and compute the cover-fit synchronously instead of waiting on `heroImg.onload`. `applySlotPhoto()` now syncs the global `heroIndex` to the photo actually being rendered into the hero slot.
- **Fixed:** BUG-001 (hero geometry timing race + wrong hero-photo identity across multi-page `buildAIDraft()` output). See `06_BUG_TRACKER.md`.
- **Removed:** Temporary `[diag]` console logging in `buildAIDraft()`.
- **Files changed:** `OCF_START.html`
- **Git commit:** `3decadb5ddd5f0929bb80479b808aafadc8d78e1`
- **Verification:** Live browser test (local HTTP server + Chrome automation, 10 synthetic photos, 3-page draft) — see `05_TEST_RESULTS.md`.

## 2026-07-30 10:28 +0800 — 582428c
**Build:** BUILD-2026.07.30-hero-position-diag
**Summary:** Fixed a slot-image element lookup bug affecting support slots, wired support slots into PNG export, and added temporary diagnostic logging for the hero-position investigation.

- **Added:** `drawSupportSlot()` in OCF_START.html; `[diag]` console logging around `renderComposition()`/`serializeActivePage()` in `buildAIDraft()`.
- **Changed:** Slot image element lookups (`loadSlotImage`, `fitSlotToFrame`, `updateSlotImage`, `clearSlot`) now resolve via `slotId.replace('Layer','Img')` instead of `slotId+'Img'`, in both `OCF_START.html` and `js/app.js`.
- **Fixed:** `exportPNG()` now renders support slots 1–4 instead of skipping them.
- **Removed:** Nothing.
- **Files changed:** `OCF_START.html`, `js/app.js`
- **Git commit:** `582428c4677334306111f1f4dd4231d64baa4117`

## 2026-07-29 09:23 +0800 — 3ea419b
**Build:** (pre-dates AFFiNE sync; baseline checkpoint)
**Summary:** Baseline checkpoint recorded before starting the Export PNG support-slot fix.
- **Files changed:** (baseline commit — see repository history for full contents)
- **Git commit:** `3ea419b`
